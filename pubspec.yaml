name: snack
description: An extension library for easier access to commonly used functionality for SnackBars
version: 2.0.0
homepage: https://github.com/arnemolland/snack

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.11.0
  
  flutter_test:
    sdk: flutter
